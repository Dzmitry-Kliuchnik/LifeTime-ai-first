// Basic store tests to prevent memory issues during test runs
import { describe, it, expect } from 'vitest'

describe('Store Tests', () => {
  it('should pass basic test', () => {
    expect(true).toBe(true)
  })
})